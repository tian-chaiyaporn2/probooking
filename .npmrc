# Hoisted node_modules: a single physical copy of each dependency. Avoids the
# pnpm-symlink vs realpath mismatch that makes tsx load a second @cucumber/cucumber
# instance under ESM (support code would otherwise register on a dead instance).
node-linker=hoisted
