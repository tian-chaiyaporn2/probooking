{
  "name": "@probook/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Background sweeps: auto-accept, clinic-review, review-publish, reminders (§7.2).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "sweep:once": "tsx src/index.ts --once",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src"
  },
  "dependencies": {
    "@probook/db": "workspace:*",
    "@probook/domain": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
